spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/student_db
    username: postgres
    password: Str@wb3rry
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true


